━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[1] 남은 작업 전부 시키기 — 아래 줄부터 끝까지 복사해서 Codex 창에 붙여넣기
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

C:\Users\hkpark\Desktop\fabric-rnd 폴더에서 작업해줘.

먼저 docs/codex/HANDOFF.md 파일을 읽어. 지금까지 한 일과 남은 일이 정리되어 있어.

HANDOFF.md의 3번 항목에 적힌 남은 작업을 순서대로 전부 해줘:
1. docs/codex/08-rdda.md 를 읽고 assets/js/views/rdda.js 만들기
2. docs/codex/09-setting.md 를 읽고 assets/js/views/setting.js 만들기
3. 전체 점검
4. 맨 마지막에 커밋 한 번만. 푸시는 하지 마.

지킬 것:
- AGENTS.md 에 적힌 규약을 지켜줘.
- legacy 폴더는 절대 열지 마. 구버전 파일이라 시간만 낭비돼.
- assets/css 폴더 안의 파일은 고치지 마.
- 이미 완성된 화면 파일은 고치지 마: home.js, development.js, ts.js, sync.js, study.js, calendar.js
- 브라우저로 확인하려고 하지 마. 그 환경엔 브라우저가 없어. node --check 까지만 해줘.

끝나면 무엇을 만들었는지, 내가 직접 확인해야 할 게 있는지 알려줘.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2] 화면이 안 뜨거나 에러가 날 때
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

C:\Users\hkpark\Desktop\fabric-rnd 폴더에서 작업해줘.

브라우저에서 http://localhost:5173/#/study 를 열었더니 화면이 안 뜨고
콘솔에 이런 에러가 나:

(여기에 F12 눌러서 나온 빨간 글씨를 그대로 붙여넣기)

이 문제만 고쳐줘. 해당 화면 파일 하나만 고치고 다른 파일은 건드리지 마.
docs/codex/HANDOFF.md 의 5번 "이미 밟은 지뢰" 목록을 먼저 확인해줘.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[3] 브라우저가 아예 멈출 때 (먹통, 에러도 안 뜸)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

C:\Users\hkpark\Desktop\fabric-rnd 폴더에서 작업해줘.

(화면 이름) 화면을 열면 에러도 없이 브라우저가 완전히 멈춰.

docs/codex/HANDOFF.md 5번 지뢰 목록의 1번을 확인해줘.
MutationObserver 로 자기가 고칠 DOM 을 감시하는 코드가 있으면
createTable 의 onRender 훅으로 바꿔줘. 그 파일 하나만 고쳐.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[4] 보기 좋게 고쳐달라고 할 때
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

C:\Users\hkpark\Desktop\fabric-rnd 폴더에서 작업해줘.

(화면 이름) 화면에서 (무엇이) (어떻게) 보였으면 좋겠어.

그 화면 파일 안의 STYLE_TEXT 부분만 고쳐줘.
색은 tokens.css 에 있는 변수만 쓰고, assets/css 폴더는 건드리지 마.
